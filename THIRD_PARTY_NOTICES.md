# Third-Party Notices

This repository interacts with or depends on upstream projects and products. Those projects remain subject to their own licenses and terms.

## Claude Code

Claude Code is not bundled in this repository. This repository provides a plugin/runtime adapter intended to be loaded by a local Claude Code installation.

## Model Context Protocol SDK

This project depends on `@modelcontextprotocol/sdk` through npm. That dependency remains under its upstream license.

## zod

This project depends on `zod` through npm. That dependency remains under its upstream license.

## Other Dependencies

Additional transitive dependencies installed by `npm install` remain under their respective upstream licenses and notices.
