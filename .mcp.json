{
  "mcpServers": {
    "jue": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/server/index.mjs"
      ],
      "cwd": "${CLAUDE_PLUGIN_ROOT}",
      "env": {
        "JUE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
